import SiteFrame from "@/components/SiteFrame";

export default function AdminPage() {
  return (
    <SiteFrame>
      <div className="card">
        <h1>Admin Console</h1>
        <p>Internal administrators now use the replacement portal. This legacy landing page remains for route compatibility.</p>
      </div>
    </SiteFrame>
  );
}
